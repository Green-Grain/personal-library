.book__right__option__evaluation
  .book__right__option__evaluation--positive
    = fa_icon 'thumbs-up', class: 'icon'
    -# controller で集計した値を表示しよう
    (T.B.D.)
  .book__right__option__evaluation--negative
    = fa_icon 'thumbs-down', class: 'icon'
    (T.B.D.)
- if user_signed_in?
  -# 自分の書架にない場合は登録、ある場合は非表示
  - unless Book.get_book_by_user(current_user.id, book[:id])
    .book__right__option__bookmark--enable
      = link_to add_shelf_book_path(book[:id]), method: 'post' do
        = fa_icon 'bookmark'
        My書架に追加
  - else
    .book__right__option__bookmark--disable
      My書架に登録済み
  .book__right__option__edit
    = fa_icon 'edit'
    評価を編集
.book__right__option__link
  = link_to "#{book[:link_url]}" do
    .book__right__option__link--rakuten
      = fa_icon 'external-link'
      楽天ブックス
  -# .book__right__option__link--amazon
  -#   LinkAMZN
